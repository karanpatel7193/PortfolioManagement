export enum DatePickerMode { 
    Month = "month", 
    Date = "date"
  } 

export enum DayOfMonth { 
    Start = "start", 
    End = "end"
}

export enum enumExport {
  Excel = 1,
  PDF = 2,
  Word = 3
}

export enum transactionType {
  Buy = 3001,
  Sell = 3002,
  Dividened = 3003 
}
